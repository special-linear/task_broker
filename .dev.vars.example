# Nonfunctional placeholders. Configure real values in Cloudflare's dashboard.
APP_SIGNING_SECRET=replace-with-at-least-32-random-bytes
# INSTANCE_EPOCH is a public recovery generation; preserve it during ordinary updates.
INSTANCE_EPOCH=replace-with-random-uuid
